import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import { Router, Route, Link, browserHistory, IndexRoute } from 'react-router'

class Next extends Component {
    
  render(){
    return(
      <div>Hi, I am login now</div>
    )
  }
    
}
export default Next;